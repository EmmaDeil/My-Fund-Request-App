{
  "name": "fund-request-dashboard-web",
  "version": "1.0.0",
  "description": "Web-based Fund Request Management Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --production",
    "dev": "nodemon server.js",
    "deploy": "npm install --production && node server.js",
    "test": "node --version && npm --version"
  },
  "keywords": [
    "fund-request",
    "dashboard",
    "web",
    "mongodb",
    "express",
    "analytics"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.18.2",
    "pdfkit": "^0.17.2",
    "multer": "^1.4.5-lts.1",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}